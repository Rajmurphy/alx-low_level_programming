#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (SUCCESS)
 **/

int main(void) 
{
       char alpha;
           for (alpha = 'a'; alpha <= 'z'; alpha ++) 
              {
              putchar('\n');
              }
        return (0);
}              
